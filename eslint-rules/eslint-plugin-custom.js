// eslint-plugin-example.js

const newLineBeforeSubscribe = require("./new-line-before-subscribe");
const plugin = { rules: { "new-line-before-subscribe": newLineBeforeSubscribe } };

module.exports = plugin;
